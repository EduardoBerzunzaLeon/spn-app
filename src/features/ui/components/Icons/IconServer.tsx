import { IconServer as IconServerTabler } from '@tabler/icons-react';
import { IconAppProps } from './icon.types';

export const IconServer = (props: IconAppProps) => {
  return <IconServerTabler {...props} />;
};
